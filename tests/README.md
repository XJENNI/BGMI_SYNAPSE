Run E2E tests locally:

1. Install dependencies:

   npm install

2. Start the static server (serves the project root at http://127.0.0.1:8080):

   npm start

3. Run Playwright tests:

   npm test

CI: GitHub Actions will run tests automatically on push/PR to `main`.